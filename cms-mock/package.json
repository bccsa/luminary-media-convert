{
    "name": "@luminary-media-converter/cms-mock",
    "version": "0.0.1",
    "license": "Apache-2.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build:libs": "(cd .. && npm -w hls-core run build)",
        "build": "npm run build:libs && vue-tsc -b && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "@luminary-media-converter/hls-core": "file:../hls-core",
        "vue": "^3.5.0"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^5.2.0",
        "typescript": "~5.7.0",
        "vite": "^6.1.0",
        "vue-tsc": "^2.2.0"
    }
}
