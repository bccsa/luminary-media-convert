node_modules
dist
dev-dist
coverage
files
processed
temp
work
.env
.DS_Store
.vscode
.wrangler
.claude/worktrees/
.cursor/**/*.log
dispatchQueue.json
jobQueue.json
tusd/bin/
# Desktop build output, and the ffmpeg binaries that are built rather than
# committed (see app-electron/bin/README.md).
app-electron/release/
app-electron/bin/*/
# …except the licence texts, which are a few KB of text and must ship whether or
# not anyone reruns a build. The GPL obliges a *copy* of the licence to travel
# with the binary; a URL in a notice is not a copy.
!app-electron/bin/licenses/
!app-electron/bin/licenses/*
*.tsbuildinfo
docs/todo.md

DESIGN.md

test-media/*
ffmpeg-build/.work/
