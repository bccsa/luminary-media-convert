/**
 * `videojs-youtube` registers a `youtube` tech as an import side effect and
 * exposes nothing worth typing — the player is driven entirely through
 * `player.src({ type: 'video/youtube', src })` afterwards. A bare declaration
 * is what the Luminary app carries too.
 */
declare module 'videojs-youtube';
