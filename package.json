{
    "name": "luminary-media-converter",
    "private": true,
    "license": "Apache-2.0",
    "workspaces": [
        "api",
        "app",
        "encode-config",
        "tusd"
    ],
    "scripts": {
        "dev": "concurrently -k -n ecfg,api,app -c green,blue,magenta \"npm -w encode-config run dev\" \"npm -w api run start:dev\" \"npm -w app run dev\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
