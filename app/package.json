{
    "name": "@luminary-media-converter/app",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build:libs": "(cd .. && npm -w hls-core run build && npm -w encode-config run build && npm -w player-core run build && npm -w player-web run build)",
        "build": "npm run build:libs && vue-tsc -b && vite build",
        "preview": "npm run build && vite preview",
        "test": "vitest run"
    },
    "dependencies": {
        "@luminary-media-converter/encode-config": "file:../encode-config",
        "@luminary-media-converter/hls-core": "file:../hls-core",
        "@luminary-media-converter/player-core": "file:../player-core",
        "@luminary-media-converter/player-web": "file:../player-web",
        "vue": "^3.5.0",
        "vue-router": "^4.6.4"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "@vitejs/plugin-vue": "^5.2.0",
        "@vue/test-utils": "^2.4.6",
        "jsdom": "^29.0.2",
        "tailwindcss": "^4.0.0",
        "typescript": "~5.7.0",
        "vite": "^6.1.0",
        "vitest": "^4.0.18",
        "vue-tsc": "^2.2.0"
    }
}
